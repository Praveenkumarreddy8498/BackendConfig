server:
  port: 8081
url:
  path: "http://localhost:8983/solr/bus"
eureka:
  client:
    service-url:
      default-zone: http://EUREKA-SERVER 
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
