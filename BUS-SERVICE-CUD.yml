server:
  port: 8081
eureka:
  client:
    service-url:
      default-zone: http://EUREKA-SERVER 
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
