server:
  port: 8082
eureka:
  client:
    service-url:
      default-zone: http://EUREKA-SERVER 
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
db:
  path: http://localhost:8983/solr/bus
